import 'package:flutter/material.dart';

class Cerca extends StatelessWidget {
  const Cerca({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Cerca page'),
    );
  }
}
